import { SearchManagerPipe } from './search-manager.pipe';

describe('SearchManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
